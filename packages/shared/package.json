{
    "name": "@openAutomate/shared",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "test": "echo \"No tests for @openAutomate/shared\"",
        "lint": "eslint src/",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
